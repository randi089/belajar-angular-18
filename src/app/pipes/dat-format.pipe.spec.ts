import { DatFormatPipe } from './dat-format.pipe';

describe('DatFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DatFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
